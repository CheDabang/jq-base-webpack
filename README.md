# 背景

所在公司会时不时接到单独的静态页面需求，（虽然有些时候隔一段时间会又继续扩展。）

而给我的时间却是有限，所有弄一个webpack的基础版本，用来直接撸jq。

# npm run dev
运行开发环境

# npm run dist
直接生产可以运行相对路径文件包

# 小结
目前第三方js，都是放到lib文件夹下面，然后通过webpack直接复制到dist文件当中去。

用法可能太粗暴，但是凑合现有需求。

缺点： 目前采用的是直接引入的方式，后续如果需要动态更换文件路径，或者CDN的时候，得需要手动更换。

hash值一类的也没有加，感觉完全像杀鸡用牛刀。emmmmm